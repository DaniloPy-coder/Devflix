[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "unzip"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader && php artisan migrate --force"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public/"